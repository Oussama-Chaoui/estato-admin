import { CrudLabels } from '@common/defs/types';

const Labels: CrudLabels = {
  CreateNewOne: 'Créer un nouveau tag',
  NewOne: 'Nouveau tag',
  ReadAll: 'Liste des tags',
  Items: 'Tags',
  EditOne: 'Éditer le tag',
};

export default Labels;
